---
description: Fan work out to subagents with strict briefs and converge once
---
Use the project-orchestration skill.

Fan out: $ARGUMENTS

For each subagent write a brief using the standard template: TASK, CONTEXT FILES (explicit, minimal), OUTPUT (exact paths), CONSTRAINTS, ACCEPTANCE, DO NOT.

Route mechanical work to the cheap model, judgment work stays with you. Never delegate scroll rhythm, visual coherence, or integration. Run agents in parallel, then integrate yourself in a single pass. One retry maximum per agent.

Show me the briefs before dispatching.
