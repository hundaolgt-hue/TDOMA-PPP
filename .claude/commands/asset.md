---
description: Generate a video/frame-sequence generation prompt plus its render spec
---
Use the cgi-loop-asset-prompting skill.

Produce the generation prompt AND the render spec for: $ARGUMENTS

Classify the asset type first (ambient loop / scrub sequence / trigger clip). Deliver both artifacts - never the prompt alone. Include the portrait variant spec and the verification checklist.
