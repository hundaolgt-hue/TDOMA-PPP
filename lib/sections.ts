// Section scroll runways (vh). One place, so the logo choreography and the
// section shells can never disagree.
export const HERO_VH = 320;

// Logo choreography, in units of viewport-heights of absolute scroll:
// reveal (fade in, centered) over the hero's final frames, then dock to the
// top-left corner as the company section arrives.
export const LOGO_REVEAL_START_VH = 2.0;
export const LOGO_REVEAL_END_VH = 2.7;
export const LOGO_DOCK_START_VH = 2.9;
export const LOGO_DOCK_END_VH = 3.7;
