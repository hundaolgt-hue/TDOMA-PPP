---
description: Verify the build against GOAL.md line by line with evidence
---
Use the project-orchestration skill and the award-web-experience skill.

Audit against `GOAL.md`. For each done-criterion: state PASS/FAIL and attach the evidence. Anything unverifiable is FAIL.

Then run the pre-submission checklist: fast scroll, reverse scroll, mid-page refresh, resize during pin, 4G throttle, reduced-motion, screenshot at arbitrary positions. Report the performance budget table with measured numbers.

No summary adjectives. Numbers and evidence only.
